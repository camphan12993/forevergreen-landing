---
layout: base.liquid
---
<!-- projects -->
<section class='container section mt-64'>
  <h3 class='text-3xl mb-8'>PROJECTS</h3>
  <div class='projects flex'>
    <div class='grid-col sm:mr-8 md:mr-10 flex-1'></div>
    <div class='grid-col mr-6 sm:mr-8 md:mr-10 flex-1 hidden md:block'></div>
    <div class='grid-col mr-6 sm:mr-8 md:mr-10 flex-1 hidden lg:block'></div>
    <div class='grid-col flex-1 hidden sm:block'></div>
    {% for item in projects.items %}
      <div class='grid-item w-full mb-6 sm:mb-8 md:mb-10' onclick='openModal({{item.images}},"{{settings.cloudinary_url}}")'>
        <div class='w-full relative project-item rounded-lg overflow-hidden'>
          <img src="{{settings.cloudinary_url}}{{item.images[0]}}" alt='' class='w-full '>
          <div class='absolute inset-0 bg-black bg-opacity-40 opacity-0 transition duration-300 flex items-center justify-center'>
            <h4 class='text-white font-bold text-xl'>{{item.name}}</h4>
          </div>
        </div>

      </div>
    {% endfor %}

  </div>
</section>